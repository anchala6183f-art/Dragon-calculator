# Dragon Calculator

A pure HTML/CSS/JS website calculator.
Google AdSense has been integrated into all pages.

Note for previewing: A minimal Android wrapper has been added just to make the AI Studio preview work (which requires Android). If you download the ZIP, the HTML files are completely standalone and do not require Android.
